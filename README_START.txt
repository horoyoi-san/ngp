Приватный Сервер Дремки — client 4229938

Запуск:
  1. Установить Node.js LTS и .NET 8 SDK.
  2. Запустить START.cmd от обычного пользователя — сервер сам запросит права администратора, если они нужны.

Что оставлено в runtime:
  - вход в мир;
  - смена персонажа;
  - позиция персонажа сохраняется при переключении;
  - оружие/бой;
  - безопасные web/traversal-баффы;
  - обычные console/packet/proxy логи.

FastPatch:
  - заменяет development/confidential подпись на «นี่คือเวอร์ชั่น DEV ที่ไม่ได้รับคุณภาพจากเกม Ananta GAY»;
  - скрывает индикатор несовпадения версии;
  - не подменяет gameplay Lua.

Консоль:
  - bootstrap/build/config-проверки пишутся в logs, но не засоряют окно при запуске;
  - после [READY] runtime-логи продолжают отображаться как обычно.

Откат hosts/certificate:
  Ananta.Proxy\REMOVE_PROXY_HOSTS_AS_ADMIN.ps1
